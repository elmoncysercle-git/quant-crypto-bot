name: trade

on:
  schedule:
    - cron: "0 */1 * * *"  # every 1 hours
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # allow committing state/state.json back to the repo
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true  # keep token for pushing
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/checkout@v4
        with:
          clean: true

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Only Actions installs the quantum deps:
          if [ -f requirements-quantum.txt ]; then pip install -r requirements-quantum.txt; fi
          
      - name: Sanity print files
        run: |
          echo "==== GIT HEAD ===="
          git rev-parse HEAD
          echo "==== TREE ===="
          ls -R
          echo "==== config.yml ===="
          sed -n '1,120p' config.yml || true
          echo "==== bot/trade.py (first 120 lines) ===="
          sed -n '1,120p' bot/trade.py || true
          
      - name: Show trade.py content
        run: |
          echo "==== bot/trade.py head ===="
          head -n 40 bot/trade.py

      - name: Trade
        env:
          EXCHANGE_KEY: ${{ secrets.EXCHANGE_KEY }}
          EXCHANGE_SECRET: ${{ secrets.EXCHANGE_SECRET }}
          EXCHANGE_PASSWORD: ${{ secrets.EXCHANGE_PASSWORD }}
          DWAVE_API_TOKEN: ${{ secrets.DWAVE_API_TOKEN }}
        run: |
          python -m bot.trade

      - name: Commit state (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -f state/state.json ]; then
            git add state/state.json
            git diff --cached --quiet || git commit -m "Update state [skip ci]"
            git pull --rebase origin main || true
            git push origin HEAD:main || true
          fi
